package day26polymorphism;

public class Bird extends Animal {
    /*
    - MOR
    - if the parent class has a wider access m. ==> cannot to be narrower
    - e.g. parent class is default then child class can be ==> default, protecte and public


     */
}
